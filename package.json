{
  "name": "fx-journal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve . -s -l ${PORT:-3000}",
    "dev": "npx serve . -s -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
